import { css } from 'glamor';

const styles = {
  root: css({
    textAlign: 'right',
    padding: '1em',
    backgroundColor: '#dcdcdc',
  }),

  link: css({
    color: '#333',
    textDecoration: 'none',
  }),
};

export default styles;
